import 'package:flutter/material.dart';

class ChatPutScreen extends StatelessWidget {
  const ChatPutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Chat Put Screen"),
      ),
    );
  }
}
